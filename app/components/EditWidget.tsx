import VerticalTabs from "./VerticalTabs";

const EditWidget = () => {
  return (
    <div className="w-full h-screen bg-backGray pl-5 py-[50px] flex justify-between gap-10">
      <VerticalTabs />
    </div>
  );
};

export default EditWidget;
